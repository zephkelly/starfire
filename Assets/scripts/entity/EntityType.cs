namespace Starfire.Entity
{
    public enum EntityType : byte
    {
        Ship = 0,
        Station = 1,
        Asteroid = 2,
        Debris = 3,
        Projectile = 4,
        Star = 5,
    }
}